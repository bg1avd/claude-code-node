# 🛠️ QQ Bot 集成问题修复指南

## 问题现象

运行 `cc-node --version` 时出现：

```
Error [ERR_MODULE_NOT_FOUND]: Cannot find module
'/usr/lib/node_modules/@raolin2025/claude-code-node/tools/qqbot-channel-api.js'
```

或

```
Error [ERR_MODULE_NOT_FOUND]: Cannot find module
'/home/raolin/.openclaw/workspace/claude-code-node/src/src/channel/qqbot-enhanced.js'
```

---

## 问题原因

**v2.5.0 版本发布时**：
- QQ Bot 工具文件放在项目根目录 `tools/` 下
- 但 `package.json` 的 `files` 配置只包含 `src/`
- 导致 npm 包中缺少这些工具文件，安装后报错

**v2.5.1 版本**：
- 已将工具移入 `src/tools/` 目录
- 但 `src/tools/qqbot-media.js` 中有一处导入路径错误：
  ```js
  // ❌ 错误
  import { QQBotEnhanced } from '../src/channel/qqbot-enhanced.js'
  // ✅ 正确
  import { QQBotEnhanced } from '../channel/qqbot-enhanced.js'
  ```

**v2.5.2 版本**：
- 修复了所有导入路径问题
- 包结构完整 ✅

---

## ✅ 解决方案

### 方案一：用户已通过软链接使用（推荐）

如果你使用的是 `~/bin/cc-node` 软链接（指向源码）：

1. 确认链接指向最新代码：
```bash
ls -la ~/bin/cc-node
# 应该指向: ~/.openclaw/workspace/claude-code-node/src/index.js
```

2. 更新代码到最新版本：
```bash
cd ~/.openclaw/workspace/claude-code-node
git pull origin master
```

3. 测试运行：
```bash
cc-node --version
# 应该输出: 2.5.2
```

### 方案二：通过 npm 全局安装

```bash
# 1. 卸载可能存在的旧版本
npm uninstall -g @raolin2025/claude-code-node

# 2. 清除 npm 缓存
npm cache clean --force

# 3. 安装最新版本
npm install -g @raolin2025/claude-code-node@latest

# 4. 验证
cc-node --version
# 应输出: 2.5.2
```

### 方案三：卸载并重建软链接

如果方案一无效，重建软链接：

```bash
# 1. 删除旧链接
rm ~/bin/cc-node

# 2. 创建新链接（确保目标目录已更新到最新）
ln -s ~/.openclaw/workspace/claude-code-node/src/index.js ~/bin/cc-node

# 3. 确保 ~/bin 在 PATH 中
echo $PATH | grep -q "/home/raolin/bin" || export PATH="$HOME/bin:$PATH"

# 4. 测试
cc-node --version
```

---

## 🔍 验证安装

### 检查包内容（npm 安装）

```bash
# 查看已安装的文件
ls $(npm root -g)/@raolin2025/claude-code-node/src/tools/ | grep qqbot

# 应该显示 4 个文件：
# qqbot-channel-api.js
# qqbot-media.js
# qqbot-remind.js
# qqbot-tools-wrapper.js
```

### 测试 QQ Bot 功能

```bash
# 配置环境变量
export CC_NODE_CHANNEL_QQBOT_APPID=你的AppID
export CC_NODE_CHANNEL_QQBOT_SECRET=你的AppSecret
export CC_NODE_CHANNEL_QQBOT_TARGET_GROUP=你的群OPENID

# 启动
cc-node

# 在 QQ 群中 @机器人，应该能收到回复
```

---

## 📦 版本历史

| 版本 | 状态 | 说明 |
|------|------|------|
| 2.5.0 | ❌ 有问题 | 工具文件不在 `src/` 下，npm 包缺失 |
| 2.5.1 | ⚠️ 部分修复 | 工具移入 `src/tools/`，但 `qqbot-media.js` 导入路径错误 |
| 2.5.2 | ✅ 完全修复 | 所有导入路径正确，包结构完整 |

---

## 🎯 当前正确状态

**npm 最新版本**: `@raolin2025/claude-code-node@2.5.2`
**GitHub 最新提交**: `c74837f` (fix: correct import path)
**发布日期**: 2025-05-31 15:00 (UTC+8)

包内结构：
```
@raolin2025/claude-code-node/
├── src/
│   ├── channel/
│   │   ├── qqbot-account-manager.js
│   │   ├── qqbot-enhanced.js
│   │   └── index.js (modified)
│   ├── tools/
│   │   ├── qqbot-tools-wrapper.js
│   │   ├── qqbot-channel-api.js
│   │   ├── qqbot-media.js
│   │   └── qqbot-remind.js
│   ├── core/config.js (modified)
│   └── tools/index.js (modified)
└── package.json
```

所有 QQ Bot 相关文件都在 `src/` 目录下，npm 包包含完整功能。

---

## 💡 预防措施

### 对于开发者（我）

以后发布 npm 包前，必须：
1. 运行 `npm pack --dry-run` 检查包内容
2. 确保 `package.json` 的 `files` 字段包含所有必要目录
3. 使用 `npm pack` 生成tar包后，解压验证结构

### 对于用户（你）

安装后验证：
```bash
# 1. 检查版本
cc-node --version

# 2. 查看帮助（应该正常显示，不报错）
cc-node --help | head -20

# 3. 检查工具是否注册成功
# 启动 cc-node 后尝试调用工具
# /工具 qqbot_list_guilds
# 应该显示工具列表，不报错
```

---

如果按照上述步骤操作后仍有问题，请提供：
1. `cc-node --version` 输出
2. `npm list -g @raolin2025/claude-code-node` 输出
3. 错误信息的完整堆栈

---

**修复完成时间**: 2025-05-31 15:00  
**修复版本**: v2.5.2  
**提交**: c74837f

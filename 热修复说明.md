# 🚨 QQ Bot 集成热修复说明

**问题**: 安装后报错 `Cannot find module '/usr/lib/node_modules/@raolin2025/claude-code-node/tools/qqbot-channel-api.js'`

**原因**: 初次发布 (v2.5.0) 时，QQ Bot 工具文件放在项目根目录的 `tools/` 文件夹，但 `package.json` 的 `files` 配置只包含 `src/`，导致 npm 包中缺少这些工具。

**修复**: 已发布 v2.5.1，将所有工具移入 `src/tools/` 目录，包结构完整。

---

## 🔧 用户操作

### 升级到最新版本

```bash
# 卸载旧版本（可选）
npm uninstall -g @raolin2025/claude-code-node

# 安装新版本
npm install -g @raolin2025/claude-code-node@latest

# 验证
cc-node --version
# 应输出: 2.5.1
```

### 如果仍有问题

1. 检查安装路径：
```bash
npm root -g
# 应该显示全局 node_modules 路径
```

2. 验证包内容：
```bash
ls $(npm root -g)/@raolin2025/claude-code-node/src/tools/ | grep qqbot
# 应该显示 4 个文件:
# qqbot-channel-api.js
# qqbot-media.js
# qqbot-remind.js
# qqbot-tools-wrapper.js
```

3. 如果文件缺失，尝试清除 npm 缓存重装：
```bash
npm cache clean --force
npm install -g @raolin2025/claude-code-node@2.5.1
```

---

## 📦 版本历史

| 版本 | 状态 | 说明 |
|------|------|------|
| 2.5.0 | ❌ 有问题 | 工具文件不在 `src/` 下，npm 包缺失 |
| 2.5.1 | ✅ 修复 | 工具移入 `src/tools/`，包完整 |

---

## 🎯 确认修复

安装后运行：

```bash
cc-node --help
```

应该能看到正常的帮助输出，而不是模块找不到错误。

然后可以测试 QQ Bot 功能：

```bash
# 配置环境变量
export CC_NODE_CHANNEL_QQBOT_APPID=你的AppID
export CC_NODE_CHANNEL_QQBOT_SECRET=你的AppSecret
export CC_NODE_CHANNEL_QQBOT_TARGET_GROUP=你的群OPENID

# 启动
cc-node
```

在 QQ 群中 @机器人，应该能得到回复。

---

**发布时间**: 2025-05-31 14:20 (UTC+8)  
**提交**: 1e417e0  
**版本**: 2.5.1
